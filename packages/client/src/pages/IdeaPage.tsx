import { PageFrame } from '~/components/domain/PageFrame';

export default function IdeaPage() {
    return (
        <PageFrame
            title="Idea"
            description="Idea page migration target. Random keyword generation logic will be connected during infra port."
        />
    );
}
