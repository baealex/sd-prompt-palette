import { PageFrame } from '~/components/domain/PageFrame';

export default function ImageLoadPage() {
    return (
        <PageFrame
            title="Image Load"
            description="Image metadata parsing and collection save workflow will be ported with API integration."
        />
    );
}
