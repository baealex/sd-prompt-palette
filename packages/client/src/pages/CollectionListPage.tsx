import { PageFrame } from '~/components/domain/PageFrame';

export default function CollectionListPage() {
    return (
        <PageFrame
            title="Collection"
            description="Collection list/search/delete/rename behavior will be ported after shared API and domain components."
        />
    );
}
