import { PageFrame } from '~/components/domain/PageFrame';

export default function HomePage() {
    return (
        <PageFrame
            title="Home"
            description="Home page migration target. Category/keyword CRUD and ordering will be ported in later phases."
        />
    );
}
